# Biblioteca Flask + KeyDB

## Instalación
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt

Copiar .env.example a .env

## Ejecutar
python app.py
Abrir navegador en http://localhost:5000
